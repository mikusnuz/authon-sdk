{
  "name": "authon-nuxt-example",
  "private": true,
  "scripts": {
    "predev": "pnpm --filter @authon/nuxt build",
    "dev": "nuxt dev",
    "prebuild": "pnpm --filter @authon/nuxt build",
    "build": "nuxt build",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@authon/nuxt": "workspace:*",
    "@authon/js": "workspace:*",
    "@authon/shared": "workspace:*",
    "nuxt": "^3.13.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
